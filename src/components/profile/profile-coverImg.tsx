"use client"
import React from 'react'

const ProfleImages = () => {
  return (
    <div className='w-full'>
      hello user cover and profile image section
           {/* <div className="relative h-48 overflow-hidden rounded-t-lg">
          <img src={coverImage || "/placeholder.svg"} alt="Cover" className="object-cover w-full h-full" />
          {isEditing && (
            <Button
              size="icon"
              variant="secondary"
              className="absolute top-4 right-4 bg-dark-400/80 hover:bg-dark-500/80"
              onClick={() => handleImageUpload("cover")}
            >
              <Camera className="w-4 h-4" />
            </Button>
          )}
        </div>

      
        <div className="relative mx-6 -mt-12">
          <Avatar className="w-24 h-24 border-4 border-dark-400">
            <AvatarImage src={profileImage} alt="Profile" />
            <AvatarFallback className="text-2xl bg-green-500 text-dark-300">
              {profileData.name
                .split(" ")
                .map((n) => n[0])
                .join("")}
            </AvatarFallback>
          </Avatar>
          {isEditing && (
            <Button
              size="icon"
              variant="secondary"
              className="absolute bottom-0 right-0 bg-dark-400/80 hover:bg-dark-500/80"
              onClick={() => handleImageUpload("profile")}
            >
              <Camera className="w-4 h-4" />
            </Button>
          )}
        </div> */}
    </div>
  )
}

export default ProfleImages 